function Simpsons() {
    return (
        <>
            {/* <h1>Os meus Simpsons favoritos</h1>
            {
                simpsonsList.docs.map(item => 
                    <div key={item.id}>
                        <h2>{item.name}</h2>
                        <img src={item.source} alt="" />
                        <p>{item.descricao}</p>
                        <p>{item.status}</p>
                    </div>
                )
            } */}
        </>
    )
}

export default Simpsons